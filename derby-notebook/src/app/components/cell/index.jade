index:(element="j-cell")
  .cell(
    class="{{if currentCell == cell.id}}selected{{/}}"
    on-click="selectCell cell.id"
  )
    .input
      .prompt.input_prompt
      .inner_cell
        .input_area
          j-editor(value='{{cell.source}}')
    .widget-area
    .output
    
