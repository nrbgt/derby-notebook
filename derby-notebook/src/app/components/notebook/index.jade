index:(element="j-notebook")
  #notebook_panel
    #notebook
      #notebook-container.container
        each cells as #cell, #index
          j-cell(
            cell='{{#cell}}'
            index='{{#index}}'
            currentCell='{{currentCell}}'
            userId='{{userId}}'
          )
      .end_space
